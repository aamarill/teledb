<div class="page-numbers">
  pages:
  <% @pages.each do |page| %>
    <% if current_page == page %>
        <%= "#{page}" %>
    <% else %>
      <%= link_to "#{page}", controller: controller, action: action, searchbar_input: searchbar_input, page: page %>
    <% end %>
  <% end %>
</div>
